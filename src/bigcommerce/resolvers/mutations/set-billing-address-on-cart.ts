import { MutationResolvers } from '@mesh';

export const setBillingAddressOnCartResolver: MutationResolvers['setBillingAddressOnCart'] = {
    resolve: (_root, _args, _context, _info) => {
        // TODO
        return null;
    },
};
