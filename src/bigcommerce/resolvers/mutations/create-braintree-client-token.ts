import { MutationResolvers } from '../../../meshrc/.mesh';

export const createBraintreeClientTokenResolver: MutationResolvers['createBraintreeClientToken'] = {
    resolve: (_root, _args, _context, _info) => {
        // TODO
        return '';
    },
};
