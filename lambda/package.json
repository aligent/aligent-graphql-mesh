{
  "name": "mesh-lambda",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "prebundle": "rm -rf dist",
    "bundle": "node bundle.mjs",
    "postbundle": "cd dist && ln -s ../../src/ src && zip -r index.zip ./"
  },
  "dependencies": {
    "@aws-cdk/aws-lambda": "^1.204.0"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.119",
    "esbuild": "^0.18.16"
  }
}
