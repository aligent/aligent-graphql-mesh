import { MutationResolvers } from '@aligent/bigcommerce-resolvers';

export const shareWishlistResolver: MutationResolvers['shareWishlist'] = {
    resolve: (_root, _args, _context, _info) => {
        // TODO
        return null;
    },
};
